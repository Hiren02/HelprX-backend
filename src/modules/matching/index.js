const matchingService = require('./matching.service');
const matchingController = require('./matching.controller');
const matchingRoutes = require('./matching.routes');
const matchingValidator = require('./matching.validator');

module.exports = {
  matchingService,
  matchingController,
  matchingRoutes,
  matchingValidator,
};
